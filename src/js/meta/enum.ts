export enum stockfishStatus {
	notDownloaded = 0,
	downloading = 1,
	needReload = 2,
	ready = 3,
}

export enum stockfishRunning {
	stop = 0,
	starting = 1,
	running = 2,
}

export enum playMode {
	normal = "normal",
	pass = "pass",
	retro = "retro",
}
